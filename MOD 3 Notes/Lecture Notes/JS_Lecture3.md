setTimeout(()=> alert("hello"), 3000)




Synchronous vs Asynchronous


Synchronous => runs in order top to bottom
Asynchronous => runs after all the synchronous code completes

SYNCH ALWAYS RUNS BEFORE ASYNC

Things that are async functions . . . 


Forms want to use "submit" 
event.prefectDefault() => prevents page reloads & other default tasks


e.target

e.currentTarget