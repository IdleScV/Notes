has_secure_password
bcrypt gem

1. 
    rails g migration AddPasswordDigestToUsers password_digest:string
    rails db:migrate

2 john = User.create(username: "john" , password: "doe" )
password "doe" 


john.authenticate("doe")
True
john.authenticate("poe")
False


1. Add bcrypt gem
2.  rails g migration AddPasswordDigestToUsers password_digest:string
    rails db:migrate

3. has_secure_password in User/model


ADDITIONALLY
password_confirmation
